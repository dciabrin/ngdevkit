#!/usr/bin/make -f

%:
	dh $@

override_dh_auto_configure:
	dh_auto_configure -- --prefix=/usr --enable-external-toolchain --enable-external-emudbg --enable-external-gngeo

# do not try to strip the m68k libraries
override_dh_strip:
	true
